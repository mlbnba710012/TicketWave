@model IEnumerable<TicketWave.Repository.Entity.Order>

@{
    ViewData["Title"] = "訂單管理";
}

<div class="container-fluid">
    <div class="row mb-4">
        <div class="col-12">
            <h2><i class="fa fa-shopping-cart me-2"></i> 訂單管理</h2>
        </div>
    </div>

    <div class="card">
        <div class="card-header bg-white">
            <div class="row align-items-center g-2">
                <div class="col-md-3">
                    <h5 class="mb-0">訂單列表</h5>
                </div>
                <div class="col-md-9">
                    <form method="get" class="row g-2">
                        <!-- 搜尋關鍵字 -->
                        <div class="col-md-4">
                            <input type="text"
                                   name="searchTerm"
                                   class="form-control"
                                   placeholder="搜尋訂單編號、姓名或Email..."
                                   value="@ViewBag.SearchTerm">
                        </div>

                        <!-- 活動類型篩選 -->
                        <div class="col-md-3">
                            <select name="eventType" class="form-select">
                                <option value="">全部類型</option>
                                <option value="concert" selected="@(ViewBag.SelectedEventType == "concert")">演唱會</option>
                                <option value="sport" selected="@(ViewBag.SelectedEventType == "sport")">運動賽事</option>
                                <option value="theater" selected="@(ViewBag.SelectedEventType == "theater")">表演藝術</option>
                            </select>
                        </div>

                        <!-- 訂單狀態篩選 -->
                        <div class="col-md-3">
                            <select name="status" class="form-select">
                                <option value="">全部狀態</option>
                                <option value="0" selected="@(ViewBag.SelectedStatus == 0)">未付款</option>
                                <option value="1" selected="@(ViewBag.SelectedStatus == 1)">已付款</option>
                                <option value="2" selected="@(ViewBag.SelectedStatus == 2)">已取消</option>
                            </select>
                        </div>

                        <div class="col-md-2">
                            <button type="submit" class="btn btn-primary w-100">
                                <i class="fa fa-search"></i> 搜尋
                            </button>
                        </div>
                    </form>
                </div>
            </div>
        </div>

        <div class="card-body">
            @if (Model.Any())
            {
                <div class="table-responsive">
                    <table class="table table-hover">
                        <thead>
                            <tr>
                                <th>訂單編號</th>
                                <th>會員</th>
                                <th>活動類型</th>
                                <th>訂單金額</th>
                                <th>訂購時間</th>
                                <th>付款時間</th>
                                <th>狀態</th>
                                <th>操作</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var order in Model)
                            {
                                <tr>
                                    <td><small class="font-monospace">@order.OrderNumber</small></td>
                                    <td>
                                        <strong>@order.Member?.Name</strong><br>
                                        <small class="text-muted">@order.Member?.Email</small>
                                    </td>
                                    <td>
                                        @if (order.ConcertId.HasValue)
                                        {
                                            <span class="badge bg-success">演唱會</span>
                                        }
                                        else if (order.SportId.HasValue)
                                        {
                                            <span class="badge" style="background-color:#1a73e8;">運動賽事</span>
                                        }
                                        else if (order.TheaterId.HasValue)
                                        {
                                            <span class="badge" style="background-color:#764ba2;">表演藝術</span>
                                        }
                                    </td>
                                    <td><strong>$@order.TotalAmount.ToString("N0")</strong></td>
                                    <td>@order.CreateDate.ToString("yyyy/MM/dd HH:mm")</td>
                                    <td>
                                        @if (order.PaymentDate.HasValue)
                                        {
                                            @order.PaymentDate.Value.ToString("yyyy/MM/dd HH:mm")
                                        }
                                        else
                                        {
                                            <span class="text-muted">-</span>
                                        }
                                    </td>
                                    <td>
                                        @if (order.OrderStatus == 0)
                                        {
                                            <span class="badge bg-warning text-dark">未付款</span>
                                        }
                                        else if (order.OrderStatus == 1)
                                        {
                                            <span class="badge bg-success">已付款</span>
                                        }
                                        else
                                        {
                                            <span class="badge bg-danger">已取消</span>
                                        }
                                    </td>
                                    <td>
                                        <a asp-action="Details" asp-route-id="@order.OrderId" class="btn btn-sm btn-info">
                                            <i class="fa fa-eye"></i> 詳情
                                        </a>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>

                <div class="mt-3">
                    <p class="text-muted">共 @Model.Count() 筆訂單</p>
                </div>
            }
            else
            {
                <div class="text-center py-5">
                    <i class="fa fa-shopping-cart fa-3x text-muted mb-3"></i>
                    <p class="text-muted">
                        @if (!string.IsNullOrEmpty(ViewBag.SearchTerm as string) ||
                             !string.IsNullOrEmpty(ViewBag.SelectedEventType as string) ||
                             ViewBag.SelectedStatus != null)
                        {
                            <text>找不到符合條件的訂單</text>
                        }
                        else
                        {
                            <text>目前沒有訂單</text>
                        }
                    </p>
                </div>
            }
        </div>
    </div>
</div>
